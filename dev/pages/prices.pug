extends ./../layouts/default
include ./../../src/price/mixins

block variables
  - var title = "Ценники"

block content
  section.scene
    .grid(style="--common-element-width: auto;")
      .grid__item
        +price({
          value: 1800000,
          currency: "₽"
        })

      .grid__item
        +price({
          before: "от",
          value: 1800000,
          currency: "₽",
          after: "за штуку"
        })

  section.scene.inverse
    .grid(style="--common-element-width: auto;")
      .grid__item
        +price({
          value: 1800000,
          currency: "₽"
        })

      .grid__item
        +price({
          before: "от",
          value: 1800000,
          currency: "₽",
          after: "за штуку"
        })
