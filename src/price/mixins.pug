mixin price(data)
  .price&attributes(attributes)(class=data.isOld ? "price--old" : null)
    if data.before
      .price__before !{data.before}

    .price__value !{data.value}
    .price__currency !{data.currency}

    if data.after
      .price__after !{data.after}
